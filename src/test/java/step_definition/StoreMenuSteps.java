package step_definition;

import cucumber.api.java8.En;

public class StoreMenuSteps implements En {
    public StoreMenuSteps() {
        When("^the store add this recipe \"([^\"]*)\" in his menu$", (String arg0) -> {
        });
        Then("^the  store menu is actualize$", () -> {
        });
    }
}
